{% assign collection = collections['featured-collection-test'] %}
<div>
    {% for product in collection.products %}
        <div>
            <a href="{{ product.url | within: collection }}">
                {{ product.title }}
            </a>
            {{ product.price | money }}
            {% unless product.available %}
                <br>
                <strong>Sold out!</strong>
            {% endunless %}

            {% comment %} {% if product.available == false %}
                <br>
                <strong>Sold out!</strong>
            {% endif %} {% endcomment %}
            <a href="{{ product.url | within: collection }}">
                <img src="{{ product.featured_image.src | img_url: '300x' }}" alt="{{ product.featured_image.alt | escape }}">
            </a>
        </div>
        {% else %}
        <p>no matches for featured collection test</p>
        <p>{{ collections }}</p>
    {% endfor %}
</div>

{% schema %}
    {
        "name": "Featured Collection Test",
        "settings": [],
        "presets": [
            {
                "category": "Custom Content",
                "name": "Featured Collection Test"
            }
        ]
    }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}